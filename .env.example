DB_HOST=localhost
DB_PORT=3306
DB_NAME=ctg
DB_USER=ctg_user
DB_PASSWORD=1234

# --- Autenticação JWT ---
# Gere um segredo próprio com:
#   php -r "echo bin2hex(random_bytes(32));"
# NUNCA use este valor de exemplo em produção.
JWT_SECRET=troque-por-uma-string-longa-e-aleatoria
JWT_EXPIRA_MINUTOS=60
REFRESH_EXPIRA_DIAS=7
